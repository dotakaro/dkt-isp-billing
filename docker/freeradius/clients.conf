client localhost {
    ipaddr = 127.0.0.1
    secret = testing123
    nas_type = other
    require_message_authenticator = no
}

client localhost_ipv6 {
    ipv6addr = ::1
    secret = testing123
}

# NAS lapangan diisi dari tabel SQL `nas` (read_clients = yes).
# Jangan taruh secret produksi di file ini.
